server:
  port: 9000
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: health,info
      base-path: /
#spring:
# mvc:
#  static-path-pattern: /public
    # 默认值为 classpath:/META-INF/resources/,classpath:/resources/,classpath:/public/,classpath:/public/